import bq_config
import simpleDB
from simpleDB import Db

project_id = bq_config.project_id
if __name__ == '__main__':
    # Db.create_database("test", "US")
    # Db.copy_database("test", project_id, "test2")
    # Db.list_databases()
    # Db.delete_database("test2")
